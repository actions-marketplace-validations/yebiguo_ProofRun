## Summary

<!-- What changed and why. -->

## Test plan

<!-- What you actually ran, not just "tests pass". -->

- [ ] `go build`/`go vet`/`go test ./...`
- [ ] Ran `proofrun status` against this change — not just `go test`
