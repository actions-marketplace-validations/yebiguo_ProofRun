## Summary

<!-- What changed and why. -->

## Test plan

<!-- What you actually ran, not just "tests pass". -->

- [ ] `go build`/`go vet`/`go test ./...`
- [ ] Built the current binary and ran `proofrun run-all` (or the relevant `proofrun run <name> -- <cmd>`) against this repo, then confirmed `proofrun status` — not just `go test`
